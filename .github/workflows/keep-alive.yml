name: Keep Render backend awake

on:
  schedule:
    - cron: '*/8 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit health endpoint
        run: curl -fsS https://api.upsoma.in/
